# 安全策略 / Security Policy

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| v1.5.x | ✅ 持续维护 |
| < v1.5 | ❌ 不再维护 |

## 上报安全漏洞

如果您发现了安全漏洞，请**不要**在公开的 GitHub Issue 中披露，通过以下方式私下联系作者：

- **邮箱**：1205244490@qq.com
- **QQ 群**：1098907087（群内 @ 作者）

请在报告中包含：
1. 漏洞描述与影响范围
2. 复现步骤（如适用）
3. 受影响的版本
4. 建议的修复方案（如有）

我们会在收到报告后尽快确认并修复，修复后在 Release 说明中致谢。

## 安全使用建议

- **会话密钥**：生产环境务必设置环境变量 `FLASK_SECRET_KEY`，不要使用默认开发密钥
- **监听地址**：程序默认监听 `0.0.0.0`（所有网卡），仅本地使用时建议通过防火墙限制端口 5001 的外部访问，或修改启动参数绑定 `127.0.0.1`
- **上传文件**：程序已限制单文件最大 50MB，并对上传文件名做安全清洗；请勿在不可信网络中暴露服务
- **示例数据**：仓库中的 Excel/JSON 均为脱敏示例数据，使用前请替换为实际数据
